# AI Prompt Process

Merged into `docs/chatbot-approach.md`. That document is now the condensed source of truth for the chatbot prompt process, Claude tool calls, validation, lifecycle, and persistence.
